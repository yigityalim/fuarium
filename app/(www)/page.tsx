import React from 'react'
import { Container } from '@/components/Containers'
import { VizaImage } from '@/lib/visa'
export default async function Home(): Promise<React.ReactElement> {
    return (
        <Container>
            <h1>Home</h1>
        </Container>
    )
}
